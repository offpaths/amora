# PostHog analytics
# Set these in your Xcode scheme's Environment Variables for local development.
# The token is a public client-side key — it ships in the app binary by default.
POSTHOG_PROJECT_TOKEN=phc_...
POSTHOG_HOST=https://eu.i.posthog.com
